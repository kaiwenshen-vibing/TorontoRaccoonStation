# AGENT.md — `app/schema` (Compatibility Note)

This path exists as a compatibility shim because requests may refer to `app/schema`.

The active schema package is:
- `app/schemas`

Use `app/schemas/AGENT.md` for real schema editing rules.

